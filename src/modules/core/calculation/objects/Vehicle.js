export class Vehicle {

    constructor(id, objName, owner, status, statusChangeDate) {
        this.id = id;
        this.objName = objName;
        this.owner = owner;
        this.status = status;
        this.statusChangeDate = statusChangeDate;
    }

    calculateDate() {
        let monthBeginning = new Date(today.getFullYear(), today.getMonth());
        this.statusChangeDate < monthBeginning ? status = 13 : 
        console.log(b < c);
      }
}